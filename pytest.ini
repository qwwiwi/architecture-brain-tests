[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
markers =
    unit: Unit tests (no external deps)
    integration: Integration tests (may need scripts)
    e2e: End-to-end tests (full pipeline)
